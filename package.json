{
    "name": "grinder-grader",
    "version": "1.0.0",
    "description": "MERN stack application for MultipleBites",
    "scripts": {
      "start": "node backend/server.js",
      "build": "cd frontend && npm install && npm run build",
      "heroku-postbuild": "cd frontend && npm install && npm run build"
    },
    "dependencies": {
    },
    "engines": {
      "node": ">=14.x"
    }
  }
  